openapi: 3.0.0
info:
  title: Snapify API
  version: 1.1.0
  description: >
    API Documentation for Snapify Backend. Includes endpoints for
    Authentication, Users, Notes, Folders, and Batch PDFs.

servers:
  - url: https://snapify-api-tdn8.onrender.com/api
    description: Production Server (Render)
  - url: http://localhost:3000/api
    description: Local Development Server

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    SuccessResponse:
      type: object
      properties:
        status:
          type: string
          example: success
        message:
          type: string
          example: Operation successful
    User:
      type: object
      properties:
        id:
          type: string
        email:
          type: string
        displayName:
          type: string
        avatarUrl:
          type: string
          nullable: true
    Note:
      type: object
      properties:
        id:
          type: string
        title:
          type: string
        content:
          type: string
        folderId:
          type: string
          nullable: true
        status:
          type: string
          enum: [PENDING, ACTIONED, ARCHIVED]
    Folder:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
        type:
          type: string
          enum: [MANUAL, SMART]
    BatchDocument:
      type: object
      properties:
        id:
          type: string
        title:
          type: string
        pdfUrl:
          type: string
        folderId:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
    DashboardMetrics:
      type: object
      properties:
        total:
          type: integer
          example: 15
          description: Tổng số note
        pending:
          type: integer
          example: 5
          description: Số note chưa xử lý (PENDING)
        actioned:
          type: integer
          example: 7
          description: Số note đã xử lý (ACTIONED)
        archived:
          type: integer
          example: 3
          description: Số note đã lưu trữ (ARCHIVED)

security:
  - bearerAuth: []

paths:
  # ====================================================
  # 1. AUTHENTICATION ROUTES
  # ====================================================
  /auth/register:
    post:
      tags: [Authentication]
      summary: Register a new account
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password, displayName]
              properties:
                email:
                  type: string
                  example: user@example.com
                password:
                  type: string
                  example: snapify123
                displayName:
                  type: string
                  example: John Doe
      responses:
        "201":
          description: User registered successfully

  /auth/login:
    post:
      tags: [Authentication]
      summary: Login to the system
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email:
                  type: string
                  example: user@example.com
                password:
                  type: string
                  example: snapify123
      responses:
        "200":
          description: Login successful, returns JWT token
  /auth/forgot-password:
    post:
      tags: [Authentication]
      summary: Yêu cầu khôi phục mật khẩu (Gửi mã OTP qua Email)
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email]
              properties:
                email:
                  type: string
                  example: snapify.tester@gmail.com
      responses:
        "200":
          description:
            Yêu cầu thành công. Mã OTP đã được gửi đến email (nếu email tồn
            tại).
        "400":
          description: Lỗi định dạng email không hợp lệ.
  /auth/reset-password:
    post:
      tags: [Authentication]
      summary: Đặt lại mật khẩu mới bằng mã OTP
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, otp, newPassword]
              properties:
                email:
                  type: string
                  example: snapify.tester@gmail.com
                otp:
                  type: string
                  example: "123456"
                newPassword:
                  type: string
                  example: snapify_new_pass
      responses:
        "200":
          description: Đặt lại mật khẩu thành công
        "400":
          description: Lỗi sai OTP hoặc OTP hết hạn
  # ====================================================
  # 2. USER ROUTES
  # ====================================================
  /users/profile:
    get:
      tags: [Users]
      summary: Get user profile
      responses:
        "200":
          description: Successful operation
    put:
      tags: [Users]
      summary: Update user profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                displayName:
                  type: string
                avatarUrl:
                  type: string
      responses:
        "200":
          description: Profile updated successfully

  # ====================================================
  # 3. NOTES ROUTES
  # ====================================================
  /notes/snap:
    post:
      tags: [Notes]
      summary: Snap-to-Note (Upload image)
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                image:
                  type: string
                  format: binary
      responses:
        "201":
          description: Image processed and Note created successfully

  /notes/search:
    get:
      tags: [Notes]
      summary: Search notes by keyword
      parameters:
        - in: query
          name: q
          schema:
            type: string
          required: true
      responses:
        "200":
          description: Successful operation

  /notes:
    get:
      tags: [Notes]
      summary: Get list of notes
      parameters:
        - in: query
          name: status
          schema:
            type: string
            enum: [PENDING, ACTIONED, ARCHIVED]
        - in: query
          name: page
          schema:
            type: integer
            default: 1
            minimum: 1
          description: Số trang hiện tại (Mặc định là 1)
        - in: query
          name: limit
          schema:
            type: integer
            default: 20
            minimum: 1
          description: Số lượng ghi chú trên mỗi trang (Mặc định là 20)
      responses:
        "200":
          description: Successful operation
    post:
      tags: [Notes]
      summary: Create a manual note
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title]
              properties:
                title:
                  type: string
                  example: Meeting Notes
                content:
                  type: string
                  example: Discussed Q3 roadmap...
                folderId:
                  type: string
                  nullable: true
      responses:
        "201":
          description: Note created successfully

  /notes/{id}:
    get:
      tags: [Notes]
      summary: Get note details
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Successful operation
    put:
      tags: [Notes]
      summary: Update a note
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Note"
      responses:
        "200":
          description: Note updated successfully
    delete:
      tags: [Notes]
      summary: Delete a note
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Note deleted successfully

  /notes/{id}/categorize:
    post:
      tags: [Notes - AI]
      summary: Auto Categorize Note
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Note categorized successfully

  /notes/{id}/actions:
    get:
      tags: [Notes - AI]
      summary: Get Smart Actions
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Successful operation

  # ====================================================
  # 4. FOLDERS ROUTES
  # ====================================================
  /folders:
    get:
      tags: [Folders]
      summary: Get user folders
      responses:
        "200":
          description: Successful operation
    post:
      tags: [Folders]
      summary: Create a new folder
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                  example: October Receipts
                description:
                  type: string
      responses:
        "201":
          description: Folder created successfully

  /folders/{id}:
    get:
      tags: [Folders]
      summary: Get folder details (Includes notes list)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Successful operation
    put:
      tags: [Folders]
      summary: Update folder details
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                description:
                  type: string
      responses:
        "200":
          description: Folder updated successfully
    delete:
      tags: [Folders]
      summary: Delete a folder (Moves notes to unassigned)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Folder deleted successfully

  # ====================================================
  # 5. BATCHES (PDF SCANNER) ROUTES
  # ====================================================
  /batches/scan:
    post:
      tags: [Batches (PDF Scanner)]
      summary: Document Scanner (Upload multiple images -> 1 PDF)
      description:
        Upload an array of images (max 20). The system will check file
        signatures, convert them into a single PDF, and upload it to Cloudflare
        R2.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                images:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Array of image files (JPG, PNG)
                title:
                  type: string
                  example: Hợp đồng thuê nhà 2026
      responses:
        "201":
          description: PDF generated and saved successfully

  /batches:
    get:
      tags: [Batches (PDF Scanner)]
      summary: Get list of all PDFs
      responses:
        "200":
          description: Successful operation

  /batches/{id}:
    put:
      tags: [Batches (PDF Scanner)]
      summary: Update PDF metadata (Rename or Move to Folder)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                  example: Đổi tên Hợp đồng mới
                folderId:
                  type: string
                  nullable: true
      responses:
        "200":
          description: PDF updated successfully
    delete:
      tags: [Batches (PDF Scanner)]
      summary: Delete a PDF document
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: PDF deleted successfully

  /dashboard/metrics:
    get:
      summary: Lấy thống kê số lượng Note
      tags: [Dashboard]
      description:
        Trả về tổng số lượng Note và chi tiết theo từng trạng thái (PENDING,
        ACTIONED, ARCHIVED) của user đang đăng nhập.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Get data metrics successfully
        #   content:
        #     application/json:
        #       schema:
        #         type: object
        #         properties:
        #           status:
        #             type: string
        #             example: success
        #           data:
        #             $ref: "#/components/schemas/DashboardMetrics"
        # "401":
        #   description: Chưa đăng nhập hoặc Token không hợp lệ
        #   content:
        #     application/json:
        #       schema:
        #         type: object
        #         properties:
        #           status:
        #             type: string
        #             example: error
        #           message:
        #             type: string
        #             example: Unauthorized
        # "500":
        #   description: Lỗi Server
        #   content:
        #     application/json:
        #       schema:
        #         type: object
        #         properties:
        #           status:
        #             type: string
        #             example: error
        #           message:
        #             type: string
        #             example: Internal server error
